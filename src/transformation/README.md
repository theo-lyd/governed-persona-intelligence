# Transformation Workspace

The canonical dbt path in this repository is the top-level `dbt/` directory.

Current active project:
- `dbt/phase0`

This folder remains for non-dbt transformation code if introduced in future phases.
